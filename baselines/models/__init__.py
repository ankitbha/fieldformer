"""Baseline model definitions.

These modules intentionally exclude FieldFormer's per-query sparse-neighbor
conditioning. Baseline scripts should import models from this package instead of
defining model classes inline.
"""

